# SRS Index — Use-Case Catalog (baseline)

Generated by the **index-srs** skill from the files in this folder.
Read THIS file (not the full SRS) to locate a use case, then open only the
referenced `Section`.

Empty until index-srs runs.

| UC ID | Title | File | Section | REQ refs | Actors | Summary | Depends on | Related UCs |
|-------|-------|------|---------|----------|--------|---------|------------|-------------|
|       |       |      |         |          |        |         |            |             |

`Actors` = the roles named in the SRS for that use case (the allowed/positive set).
Roles NOT listed are the denied set for negative-access tests.

`Depends on` = directional prerequisites — UC(s) that must run/exist first (from SRS
preconditions / «include» / «extend»). The reverse direction (UCs that depend on this
one) is the impact/regression set. Distinct from `Related UCs`, which is loose
semantic relatedness for discovery.

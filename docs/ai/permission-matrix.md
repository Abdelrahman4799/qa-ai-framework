# Permission Matrix (roles × use cases)

Generated by **index-srs** from the SRS actors + the role universe in
`docs/ai/test-data-policy.md`. Re-running overwrites this file.

- `✓` = actor named in the SRS (allowed → positive test)
- `✗` = not an actor (denied → negative-access test)
- `?` = `TBD - needs team confirmation` (e.g. SRS silent, or data-scoped rule)

Data-scoped permissions (e.g. "Manager — own team only") are noted per cell;
the grid captures WHO may act, not WHICH records.

- Last updated: `none yet`
- Roles: `Admin · Manager · Supervisor · Viewer · (add others)`

| UC ID | Title | Admin | Manager | Supervisor | Viewer |
|-------|-------|-------|---------|------------|--------|
|       |       |       |         |            |        |

## Notes / TBDs
- `none yet`

# Glossary

Define project-specific terms so test cases use consistent language.
Mark unknowns `TBD - needs team confirmation`.

- SRS — Software Requirements Specification. Existing baseline: `docs/ai/srs/`.
- New-feature SRS — spec for the feature under test: `docs/ai/new-feature-srs/`.
- UC — Use Case (the unit of work; ID form `UC-###`).
- REQ — Requirement (ID form `REQ-###`).
- AC — Acceptance Criteria.
- Regression set — existing test cases for use cases related to the one under test.
- `<domain term>` — `TBD`
